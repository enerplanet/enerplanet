node_modules/
.env
# Hold the stack's database passwords and client keys. Only the .example
# files belong in the repository.
heat.env
shared.env
.env.production
.env.docker
package-lock.json
*.log
dist/
.local
build/
sonarqube-25.10.0.114319
sonar-project.properties
.scannerwork

# MkDocs build output
site/
docs/.venv/

# Auth service (separate repository)
auth-service/

# Infrastructure (separate repository)
dependencies/
